<UserControl
    x:Class="OpenNEL_WinUI.AdContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OpenNEL_WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid CornerRadius="8" Padding="12" VerticalAlignment="Top">
        <StackPanel Spacing="8">
            <TextBlock Text="广告" Style="{StaticResource SubtitleTextBlockStyle}"/>
            <Border CornerRadius="4" Padding="12" MinHeight="160" HorizontalAlignment="Stretch" VerticalAlignment="Top">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <ScrollViewer x:Name="AdScroll" Grid.Row="0" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled" ZoomMode="Disabled" Padding="48,0">
                        <StackPanel x:Name="AdStack" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </ScrollViewer>
                    <Button x:Name="PrevButton" Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0" Width="36" Height="36" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Click="PrevButton_Click">
                        <PathIcon Data="M 14 2 L 6 10 L 14 18 Z"/>
                    </Button>
                    <Button x:Name="NextButton" Grid.Row="0" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0" Width="36" Height="36" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Click="NextButton_Click">
                        <PathIcon Data="M 2 2 L 10 10 L 2 18 Z"/>
                    </Button>
                    <StackPanel x:Name="DotPanel" Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,8,0,8"/>
                </Grid>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
