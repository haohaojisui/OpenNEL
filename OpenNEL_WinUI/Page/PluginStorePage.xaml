<Page
    x:Class="OpenNEL_WinUI.PluginStorePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OpenNEL_WinUI"
    xmlns:handlers="using:OpenNEL_WinUI.Handlers.Plugin"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <local:CountToVisibilityConverter x:Key="CountToVisibilityConverter"/>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <local:BoolNegationConverter x:Key="BoolNegationConverter"/>
        <local:BoolToVisibilityNegationConverter x:Key="BoolToVisibilityNegationConverter"/>
    </Page.Resources>

    <Grid Padding="20">
        <Grid CornerRadius="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Padding="20,20,20,12" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="插件商店" Style="{StaticResource SubtitleTextBlockStyle}" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                    <Button Content="返回" Click="BackButton_Click"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1" Padding="20">
                <TextBlock Text="暂无插件" 
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center" 
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           Visibility="{x:Bind AvailablePlugins.Count, Converter={StaticResource CountToVisibilityConverter}, Mode=OneWay}"/>

                <ListView ItemsSource="{x:Bind AvailablePlugins}" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="handlers:AvailablePluginItem">
                            <Grid Padding="0,12" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Image Width="40" Height="40" Source="{x:Bind LogoUrl}" Stretch="UniformToFill" Margin="0,0,12,0"/>
                                <StackPanel Grid.Column="1" Spacing="2">
                                    <TextBlock Text="{x:Bind Name}" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                    <TextBlock Text="{x:Bind ShortDescription}" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                                    <TextBlock Text="{x:Bind Publisher}" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                    <Button Content="安装" Click="InstallAvailablePluginButton_Click" Tag="{x:Bind}" Visibility="{x:Bind IsInstalled, Converter={StaticResource BoolToVisibilityNegationConverter}, Mode=OneWay}"/>
                                    <TextBlock Text="已安装" Visibility="{x:Bind IsInstalled, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Grid>
    </Grid>
</Page>
